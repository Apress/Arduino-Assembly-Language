/*============================================================
 * A sketch to demonstrate the use of the COM instruction. In
 * this sketch, we have LEDs on each of the pins D0--D7 (PD0--
 * PD7). In the setup, we enable the even numbered pins and 
 * light up the LEDs. When the switch is pressed, we use COM
 * to invert the LEDs that are lit. Keep pressing the switch
 * and the LEDs will keep switching state.
 *
 * Norman Dunbar
 * 10 August 2024.
 *==========================================================*/

 // Nothing to see here, it all happens in the NOT.S file.
