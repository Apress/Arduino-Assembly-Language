Some Hex to ASCII conversion routines. These handle 8, 16
or 32 bit values. The Hex (ASCII) output is written to the
data location pointed to by the X register.

A C++ Interface file is supplied to allow C++ sketches access 
to the conversion routines. Although Serial.print(x, HEX) will
do a similar task.

Norman Dunbar.
24 April 2024.
